# ── Interfaz ──────────────────────────────────────────────────────
PyQt6>=6.6.0

# ── Reconocimiento de voz ─────────────────────────────────────────
# faster-whisper usa CTranslate2 en lugar de PyTorch: mismos pesos que
# openai-whisper, 4-8x mas rapido y ~2.5 GB menos de dependencias.
faster-whisper>=1.1.0

# PyAV trae ffmpeg compilado dentro del wheel, asi que no hace falta
# instalar ffmpeg en el sistema para leer audio ni video.
av>=13.0.0

# ── Diarizacion de hablantes ──────────────────────────────────────
# pyannote-segmentation-3.0 exportado a ONNX + embeddings 3D-Speaker.
# Misma arquitectura que pyannote.audio, sin PyTorch ni token de HuggingFace.
sherpa-onnx>=1.10.0

# ── OCR ───────────────────────────────────────────────────────────
rapidocr-onnxruntime>=1.3.0
Pillow>=10.0.0
PyYAML>=6.0

# PDF: extraccion de texto embebido y rasterizado de paginas escaneadas
PyMuPDF>=1.24.0

# Opcional: si el binario de Tesseract esta instalado, se usa como
# alternativa al motor ONNX. La app funciona igual sin el.
pytesseract>=0.3.10

# ── Exportacion ───────────────────────────────────────────────────
fpdf2>=2.7.0
python-docx>=1.1.0

# ── Empaquetado (solo para generar el .exe) ───────────────────────
pyinstaller>=6.0.0
